package com.example.booknook

// holds both the name of a collection and the items in it
// Olivia Fishbough
class CollectionItem (val collectionName: String,
                      val books: List<BookItemCollection> = listOf()){
}