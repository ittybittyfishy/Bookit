<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color (white) -->
    <solid android:color="#FFFFFF"/>

    <!-- Border (black with 2dp width) -->
    <stroke
        android:width="1dp"
        android:color="#8B845E"/>

    <!-- Define corner radius (optional, remove if you want sharp corners) -->
    <corners android:radius="4dp"/>
</shape>
